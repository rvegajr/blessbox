# Public environment variables for production build
# Secrets are configured in Vercel and override these during deployment
#
# Traklet is intentionally NOT enabled here: the widget is dev-only
# (local + dev.blessbox.org). Its flags live in the Vercel Development
# and Preview environments, never Production. See env.template.