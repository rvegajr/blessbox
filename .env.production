# Public environment variables for production build
# Secrets are configured in Vercel and override these during deployment

# Traklet QA Testing Widget (public variable, safe to commit)
NEXT_PUBLIC_TRAKLET_ENABLED=true
