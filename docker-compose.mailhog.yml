version: '3.8'
services:
  mailhog:
    image: mailhog/mailhog:v1.0.1
    container_name: blessbox-mailhog
    ports:
      - "1025:1025"   # SMTP
      - "8025:8025"   # Web UI
    restart: unless-stopped



